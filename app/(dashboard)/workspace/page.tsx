import FounderWorkspace from '@/components/features/FounderWorkspace'

export const metadata = { title: 'Founder Workspace — Thinkior AI' }

export default function WorkspacePage() {
  return <FounderWorkspace />
}
